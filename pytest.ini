[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_functions = test_*
# Put the repo root on sys.path so `import app...` works when pytest is invoked
# directly (e.g. in CI), not only via `python -m pytest`.
pythonpath = .
