# Pin all transitive production dependencies for reproducible deployments.
-c requirements-prod.lock

python-telegram-bot[job-queue]>=21.11
apscheduler==3.10.4
aiohttp>=3.9.0
python-dotenv>=1.0.0
SQLAlchemy>=2.0.0
aiosqlite>=0.19.0
psycopg2-binary>=2.9.0  # PostgreSQL driver (used when DATABASE_URL is a postgres:// URL)
PyJWT>=2.8.0
bcrypt>=4.1.0

fpdf2>=2.7.0

# Decoding driver document photos to check they are real, legible images and to fingerprint
# them (app/services/image_check.py). Already present transitively via fpdf2 and pinned in
# the lock files; declared here because it is now used directly, so a future fpdf2 that drops
# the dependency cannot silently break document uploads.
Pillow>=10.0.0

sentry-sdk>=2.0.0  # Optional error monitoring; inert unless SENTRY_DSN is set
