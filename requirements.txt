python-telegram-bot[job-queue]>=21.11
apscheduler==3.10.4
aiohttp>=3.9.0
python-dotenv>=1.0.0
SQLAlchemy>=2.0.0
aiosqlite>=0.19.0
psycopg2-binary>=2.9.0  # PostgreSQL driver (used when DATABASE_URL is a postgres:// URL)
PyJWT>=2.8.0
bcrypt>=4.1.0

fpdf2>=2.7.0

# Optional error monitoring. Only active when SENTRY_DSN is set (see app/services/monitoring.py).
sentry-sdk>=2.0.0
